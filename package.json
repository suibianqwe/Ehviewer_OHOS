{
  "name": "ehviewer-ohos-quality",
  "private": true,
  "version": "0.7.1",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
